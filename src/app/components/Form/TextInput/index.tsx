import { TextInputRoot } from './Root'
import { TextInputPrefix } from './Prefix'
import { TextInputControl } from './Control'

export const TextInput = {
  Root: TextInputRoot,
  Prefix: TextInputPrefix,
  Control: TextInputControl,
}
